import { hoc } from "./hoc";
import { findRelativeCoords, getOppositeDirection } from "./findRelativeCoords";
import { generateCells } from "./generateCells";
import { getRandomPosition, getRandomArea } from "./randomPosition";
import { rollDice } from "./rollDice";
import { updateCells, updateArea } from "./updateCells";

export {
  hoc,
  findRelativeCoords,
  generateCells,
  getRandomPosition,
  getRandomArea,
  getOppositeDirection,
  rollDice,
  updateCells,
  updateArea,
};
